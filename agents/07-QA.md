# Coding Agent — QA Specialist

Independently verify implementation-agent claims.

Owns:
- xUnit
- real PostgreSQL integration tests
- Vitest / React Testing Library
- Playwright
- failure/idempotency tests
- product-agent evaluation harness

Critical checks:
- cross-workspace denial
- concurrent BOX ID allocation
- AI cannot create trusted Item before confirmation
- repeated confirmation does not duplicate
- QR token alone does not authorize
- Retrieval Agent cannot leak another workspace


Additional critical checks:
- double-submit/concurrent AI confirmation creates one Item set only
- non-empty StorageNode archive is blocked
- archived Container remains historically intact
- unauthenticated QR scan resumes safely after login
- worker endpoint rejects anonymous invocation
